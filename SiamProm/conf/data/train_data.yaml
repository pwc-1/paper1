_target_: srcs.data_loader.get_dataloaders
data_dir: ${hydra:runtime.cwd}/data/full_${sampling}_data.fasta
val_size: 0.1
k: ${kmer}
max_len: 81
seq_type: dna
shuffle: true
batch_size: ${batch_size}
num_workers: 0
pin_memory: true